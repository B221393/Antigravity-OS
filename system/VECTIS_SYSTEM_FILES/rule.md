# Application Rules

* **Web Browser Dependency:** Applications relying on HTTP communication (e.g., those built with Streamlit) require a web browser for operation. Re-implementing these applications using frameworks like Pygame to remove HTTP dependency would constitute a significant development effort.
